# How to gen mkdocs


## for development
- from sefl-study repo, where *mkdoc.yml* located

```shell
docker run --rm -it -p 8000:8000 -v ${PWD}:/docs squidfunk/mkdocs-material
```

## build static site

```shell
docker run --rm -it -v ${PWD}:/docs squidfunk/mkdocs-material build
```